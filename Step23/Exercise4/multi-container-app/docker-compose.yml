services:
  backend:
    build: ./backend
    container_name: node_backend
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: next_frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    environment:
      - NEXT_DISABLE_TURBOPACK=1
